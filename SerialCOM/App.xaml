<Application x:Class="Kogler.SerialCOM.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             d1p1:Ignorable="d"
             xmlns:d1p1="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Kogler.SerialCOM">
    <Application.Resources>
        <local:ViewModelLocator x:Key="Locator" d:IsDataSource="True" />
        <ContentControl x:Key="Sessions" DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
            <TreeView ItemsSource="{Binding Sessions}">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Documents}">
                        <TextBlock Text="{Binding Port.PortName}" />
                        <HierarchicalDataTemplate.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Key}" />
                            </DataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </ContentControl>
    </Application.Resources>
</Application>